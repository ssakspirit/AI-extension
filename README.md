# AI Extension

마인크래프트 에이전트를 활용하여 AI의 개념을 체험할 수 있는 확장입니다.

## 블록 목록

### 에이전트 앞에 엔티티 감지
에이전트 바로 앞 칸에 엔티티(몹 또는 플레이어)가 있는지 확인합니다.  
엔티티가 있으면 `참(true)`, 없으면 `거짓(false)`을 반환합니다.

**동작 원리:**
- 장애물 감지 (`AgentDetection.Obstacle`) + 블록 감지 (`AgentDetection.Block`) 를 조합
- 장애물은 있지만 블록이 아닌 경우 → 엔티티로 판단
